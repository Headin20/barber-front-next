//UTILS
const IS_REQUIRED = 'Required field';
const FIELD_NOT_SPACES_ONLY = field => `${field} can not contain spaces only`;
const ARRAY_MIN = 'Select more or equal one';
const DATE_RANGE = 'Invalid date range'

//LOGIN
const NOT_EMAIL = 'This not email';


export {
    IS_REQUIRED,
    ARRAY_MIN,
    FIELD_NOT_SPACES_ONLY,
    NOT_EMAIL,
    DATE_RANGE
}